apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: portfolio-site-tls
spec:
  secretName: portfolio-site-tls
  dnsNames:
    - michael-bailey.net
    - www.michael-bailey.net
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer